###################
### KEYBINDINGS ###
###################

# Main modifier key
# $mainMod is used throughout all keybinds (set to SUPER / Windows key)
$mainMod = SUPER

############################
# WALLPAPER CONTROLS
# Launch waypaper with horizontal/vertical wallpaper folders
############################
bind = $mainMod, P, exec, waypaper --folder $wallpapers-h
bind = $mainMod SHIFT, P, exec, waypaper --folder $wallpapers-v

############################
# CORE APPLICATION & SESSION CONTROLS
# Launch apps, close windows, exit session, toggle layouts
############################
bind = $mainMod, Q, exec, $terminal        # Open terminal
bind = $mainMod, C, killactive,            # Close active window
bind = $mainMod, M, exit,                  # Exit Hyprland session
bind = $mainMod, E, exec, $fileManager     # Open file manager
bind = $mainMod, V, togglefloating,        # Toggle floating mode
bind = $mainMod, R, exec, $menu            # Open app launcher/menu
bind = $mainMod, P, pseudo,                # Toggle pseudo tiling (dwindle layout)
bind = $mainMod, J, togglesplit,           # Toggle split direction (dwindle)
bind = $mainMod, i, exec, firefox          # Launch Firefox

############################
# WINDOW FOCUS MOVEMENT
# Move focus between windows using arrow keys
############################
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

############################
# WORKSPACE SWITCHING
# Jump to workspaces 1–10
############################
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

############################
# MOVE WINDOWS TO WORKSPACES
# Send active window to a specific workspace
############################
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

############################
# SPECIAL WORKSPACE (SCRATCHPAD)
# Toggle and send windows to a hidden "magic" workspace
############################
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

############################
# WORKSPACE SCROLLING
# Cycle through workspaces using mouse scroll
############################
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

############################
# MOUSE WINDOW CONTROL
# Move and resize windows with mouse drag + modifier
############################
bindm = $mainMod, mouse:272, movewindow     # Left click drag = move
bindm = $mainMod, mouse:273, resizewindow   # Right click drag = resize

############################
# SYSTEM AUDIO & BRIGHTNESS KEYS
# Hardware keys for volume, mic, and screen brightness
############################
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

############################
# MEDIA PLAYBACK CONTROLS
# Requires playerctl for controlling media players
############################
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous
